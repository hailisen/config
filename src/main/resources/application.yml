spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/g943067313/config-repo
          username: 943067313@qq.com
          password: aiyuan1314
          basedir: /E:/configserver-dir
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/
server:
  port: 8087